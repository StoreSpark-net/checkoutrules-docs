### Método de envío seleccionado

Esta condición verifica el **método de envío seleccionado** por el cliente según criterios específicos, basado en una de las siguientes subcondiciones:

- **contiene** (Coincide cuando el método de envío seleccionado incluye el valor ingresado)
- **no contiene** (Coincide cuando el método de envío seleccionado no incluye el valor ingresado)
- **es** (Coincide con el texto exacto ingresado, o con un patrón de comodín si se usa `*`)
- **no es** (Coincide cuando el texto exacto ingresado o el patrón de comodín no coincide)

Para configurar estas condiciones, ingrese el valor en el campo y elija la comparación apropiada del menú desplegable.

> Por ejemplo, `contiene Express` → Coincide con cualquier método de envío seleccionado que incluya “Express”.

<img
  src="/images/essentials/condition-sets/selected-shipping-method-contains.webp"
  alt="Método de envío seleccionado contiene"
/>
