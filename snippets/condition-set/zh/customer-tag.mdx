### 客户标签

此条件用于根据特定标准检查**客户的标签**，基于以下子条件之一：

- **具有以下值**（当客户的标签包含输入的值时匹配）
- **不具有以下值**（当客户的标签不包含输入的值时匹配）

要设置这些条件，请在字段中输入标签值，并从下拉菜单中选择相应的比较方式。

> 例如，`具有以下值: VIP` → 匹配拥有 **VIP** 标签的客户。

<img
  src="/images/essentials/condition-sets/customer-tag-has-value-of.webp"
  alt="客户标签条件"
/>
