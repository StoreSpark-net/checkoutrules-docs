### 地址（第 1 行 + 第 2 行）

用于将客户的完整地址（由地址第 1 行和第 2 行组合而成）与一个或多个特定值、数量或地位进行比较，基于以下子条件之一：

- **包含**（如果地址包含指定的值或部分值，则匹配）
- **不包含**（如果地址不包含指定的值或部分值，则匹配）
- **为空**（如果两行都没有输入地址，则匹配）
- **不为空**（如果任意一行输入了值，则匹配）
- **数字数量大于或等于**（如果地址中的数字总数大于或等于指定的数量，则匹配）
- **数字数量小于或等于**（如果地址中的数字总数小于或等于指定的数量，则匹配）
- **字符数量大于或等于**（如果地址中的字符总数大于或等于指定的数量，则匹配）
- **字符数量小于或等于**（如果地址中的字符总数小于或等于指定的数量，则匹配）
- **单词数量大于或等于**（如果地址中的单词总数大于或等于指定的数量，则匹配）
- **单词数量小于或等于**（如果地址中的单词总数小于或等于指定的数量，则匹配）
- **仅包含**（如果地址仅包含下拉菜单中允许的字符类型，则匹配）
- **不只包含**（如果地址包含除下拉菜单中允许的字符类型之外的其他字符，则匹配）

对于 **包含** 和 **不包含**，请输入要匹配的值。

> 例如，`包含: Street` → 如果地址包含“Street”一词，则匹配。

<img
  src="/images/essentials/condition-sets/address-contains.webp"
  alt="地址 包含"
/>

对于 **仅包含** 和 **不只包含**，请从下拉菜单中选择字符类型：

- **英文字符**（仅允许 A–Z 和 a–z）
- **英文字符 + 重音变体**（允许 A–Z、a–z，以及带重音的字符，如 é、ñ、ö）
  > 例如，`仅包含: 英文字符` → 如果地址仅包含英文字符，则匹配。

<img
  src="/images/essentials/condition-sets/address-has-only.webp"
  alt="地址 仅包含"
/>

对于 **数字/字符/单词数量大于或等于** 或 **小于或等于**，请输入阈值。

> 例如，`单词数量大于或等于 5` → 如果地址包含 5 个或更多单词，则匹配。

<img
  src="/images/essentials/condition-sets/address-greater-than.webp"
  alt="地址 大于"
/>

对于 **为空** 和 **不为空**，无需输入任何值。

> 例如，`为空` → 如果地址第 1 行和第 2 行都为空，则匹配。

<img
  src="/images/essentials/condition-sets/address-is-empty.webp"
  alt="地址 为空"
/>
