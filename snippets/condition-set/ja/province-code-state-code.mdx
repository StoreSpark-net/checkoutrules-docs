### 県コード / 州コード

この条件は、チェックアウトページにある県/州フィールドの値を、1つ以上の特定の値または状態と比較するために使用されます。以下のサブ条件に基づきます：

- **いずれかの値を含む**（県フィールドがリスト内の少なくとも1つの値を含む場合に一致）
- **いずれの値も含まない**（県フィールドがリスト内の値を含まない場合に一致）
- **これら以外の値を含む**（県フィールドがリスト外の値を含む場合に一致）
- **これら以外の値を含まない**（県フィールドがリスト内の値のみで構成される場合に一致）
- **空である**（県フィールドに値がない場合に一致）
- **空でない**（県フィールドに値がある場合に一致）

**いずれかの値を含む**、**いずれの値も含まない**、**これら以外の値を含む**、**これら以外の値を含まない** の場合は、1つ以上の値を入力してください。複数の値はカンマで区切ります。

<Note>
  2文字または3文字の県/州コード（ISO_3166-2形式）を入力してください。
</Note>

> 例: `これら以外の値を含まない: CA, TX` → フィールドが「CA」または「TX」のみで構成され、その他の値を含まない場合に一致。

<img
  src="/images/essentials/condition-sets/multiple-provinces.webp"
  alt="複数の県または州"
/>

**空である** または **空でない** の場合は、値の入力は不要です。

> 例: `空でない` → フィールドに値が入力されている場合に一致。

<img
  src="/images/essentials/condition-sets/province-not-available.webp"
  alt="県または州が未入力"
/>
